# Utilities
choco install -y vlc 7zip screentogif
choco install -y google-backup-and-sync --ignore-checksums

choco install -y origin steam
choco install -y activepresenter
# Install manually:
# Google Drive, Unified Remote, FDM, VSCode, Docker for Windows, Google Cloud SDK, Xodo PDF Reader (from MS Store)

# Ray (http://myray.app), QTTabBar
